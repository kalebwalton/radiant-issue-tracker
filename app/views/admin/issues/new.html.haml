%h1 Request a New Feature or Report a Bug
- form_for :issue, @issue, :url => admin_issues_url do |f|
  .form-area
    %p.type
      = f.label :type
      ~ select_tag :label, options_for_select(['Feature', 'Bug'])
    %p.title
      = f.label :title
      = f.text_field :title, :class => 'textbox', :maxlength => 255
    %p.description
      = f.label :description
      ~ f.text_area :body, :class => "textarea", :style => "width: 100%; height: 100px;"

  %p.buttons
    = submit_tag "Submit"
    or
    = link_to 'Cancel', admin_issues_url